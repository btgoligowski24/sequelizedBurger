<li class="list-group-item list-group-item-dark d-flex font-weight-bold">
	<span class="align-self-center">{{dataValues.id}}. {{dataValues.burger_name}}</span>
	<button type="button" class="updateBurger btn btn-secondary ml-auto font-weight-bold" data-id="{{dataValues.id}}" data-newDevouredState="{{devoured}}" data-burgerName="{{dataValues.burger_name}}">
		{{#if dataValues.devoured}}MAKE ME ANOTHER!{{else}}DEVOUR IT!{{/if}}
	</button>
</li>
